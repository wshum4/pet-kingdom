<h1>All Sitters Available</h1>

<ul>
  <% @sitters.each do |sitter| %>
  <li><%= link_to sitter.first_name, sitter_path(sitter) %></li>
  <% end %>
</ul>

<%= link_to 'Home', root_path %>
<br>
<%= link_to "Log Out", destroy_user_session_path, method: :delete%>
